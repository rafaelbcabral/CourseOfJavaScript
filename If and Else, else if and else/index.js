/*
If pode ser usado sozinho.
sempre que utilizar a palavra else, tem um if antes.
Pode ter vários else ifs na checagem. (quantos eu quiser)
só pode ter um else na checagem.
pode usar condicões sem o else if, utilizando só o if e else. 
*/


const hora = 10;

if (hora >= 0 && hora <= 11) {
    console.log('bom dia');
} else if (hora >= 12 && hora <= 17) {
    console.log('boa tarde');
} else if (hora >= 18 && hora <= 23) {
    console.log('boa noite');
} else { // só será executado caso nenhuma condicão acima for verdadeira.
    console.log('Olá');
}


/* continuação de if e else, e, else if. 

*/





     


