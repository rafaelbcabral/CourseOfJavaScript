/*
operadores de comparação:

> maior que
>= maior que ou igual a
< menor que
<= menor que ou igual a 
=== igualdade estrita (valor e tipo) --> checa o tipo e o valor.
!== diferente estrito (valor e tipo) --> checa o tipo e o valor.
*/

/* 
operadores lógicos

&& -> AND = E(também pode servir também) -> todas as expressões precisam ser verdadeiras, sem nenhum false.
const expressaoAnd = true && true && true && true;

|| -> OR -> OU -> só vai retornar falso se todas elas forem falsas, sem nenhum true.
const expressaoOr = false || false || true || false;

! -> NOT -> NÃO -> inverte os valores. (!false = true)
console.log(!false);
console.log(!!!false); --> vai invertendo cada vez que colocar mais um "!".

*/


